[project]
name = "building-with-the-claude-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=1.2.0",
    "ipykernel>=7.3.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
building-with-the-claude-api = "building_with_the_claude_api:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
